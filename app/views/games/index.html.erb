<div>
  <h1> Join a Game </h1>
  <ul>
    <% @games.each do |game| %>
      <li>
        <%= link_to game.name, game_path(game) %>
      </li>
    <% end %>
  </ul>
</div>

<div>
  <%= link_to "Create a game", new_game_path(@game), class: "button" %>
</div>
